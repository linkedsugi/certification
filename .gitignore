node_modules/
.next/
out/
build/
dist/
# Local overrides only. The base .env is committed — it contains no secrets,
# only the SQLite dev DB path. Put secrets in .env.local (ignored below).
.env.local
.env*.local
*.log
.DS_Store

# Prisma local DB
prisma/dev.db
prisma/dev.db-journal
prisma/migrations/dev/

# Editor
.vscode/
.idea/
